[project]
name = "lisamindblade-backend"
version = "0.1.0"
description = "LisaMindblade assistant backend service"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7",
  "websockets>=12.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"
